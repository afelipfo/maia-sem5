[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "model-pkg"
version = "0.0.1"
description = "Local model package for API"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["model_pkg*"]
